/** Single source of truth for the referral programme rate. */
export const REFERRAL_RATE = 0.05;

/** Display string used in every referral surface (Profile row, Wallet card, Referrals page). */
export const REFERRAL_RATE_LABEL = `${Math.round(REFERRAL_RATE * 100)}%`;
